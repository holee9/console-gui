<UserControl x:Class="HnVue.UI.Views.PatientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HnVue.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1400">
    <UserControl.Resources>
        <converters:AgeFromBirthDateConverter x:Key="AgeConverter"/>
        <Style x:Key="DarkHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2f2f2f"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#585858"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="34"/>
        </Style>
        <Style x:Key="DarkRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#090909"/>
            <Setter Property="Foreground" Value="#dde6f4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#162132"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#4f79b8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SideIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
            <Setter Property="Background" Value="#1d2433"/>
            <Setter Property="BorderBrush" Value="#59657b"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#f0f4fd"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="17"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#242424" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" Margin="0,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Worklist" Foreground="White" FontSize="30" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="1" Text="&#xE76C;" FontFamily="Segoe MDL2 Assets" Foreground="#dce3f1" FontSize="26" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="3" Orientation="Horizontal">
                <Button Content="Today" Command="{Binding FilterByPeriodCommand}" CommandParameter="Today" Width="82" Height="32" Margin="0,0,6,0"/>
                <Button Content="3Days" Command="{Binding FilterByPeriodCommand}" CommandParameter="3Days" Width="82" Height="32" Margin="0,0,6,0"/>
                <Button Content="1Week" Command="{Binding FilterByPeriodCommand}" CommandParameter="1Week" Width="82" Height="32" Margin="0,0,6,0"/>
                <Button Content="All" Command="{Binding FilterByPeriodCommand}" CommandParameter="All" Width="70" Height="32" Margin="0,0,6,0"/>
                <Button Content="1Month" Command="{Binding FilterByPeriodCommand}" CommandParameter="1Month" Width="90" Height="32"/>
            </StackPanel>
            <DatePicker Grid.Column="4" Width="130" Height="32" Margin="12,0,6,0"/>
            <DatePicker Grid.Column="5" Width="130" Height="32"/>
        </Grid>

        <Grid Grid.Column="0" Grid.Row="1" Margin="0,0,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#090909" BorderBrush="#505661" BorderThickness="1">
                <DataGrid ItemsSource="{Binding Patients}"
                          SelectedItem="{Binding SelectedPatient}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#2a2a2a"
                          BorderThickness="0"
                          RowHeaderWidth="0"
                          Background="#090909"
                          Foreground="#dde6f4"
                          ColumnHeaderHeight="34"
                          RowHeight="40">
                    <DataGrid.ColumnHeaderStyle>
                        <StaticResource ResourceKey="DarkHeaderStyle"/>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <StaticResource ResourceKey="DarkRowStyle"/>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="No" Binding="{Binding PatientId}" Width="70"/>
                        <DataGridTextColumn Header="Patient ID" Binding="{Binding PatientId}" Width="210"/>
                        <DataGridTextColumn Header="Patient Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Sex" Binding="{Binding Sex}" Width="60"/>
                        <DataGridTextColumn Header="Age" Binding="{Binding DateOfBirth, Converter={StaticResource AgeConverter}}" Width="80"/>
                        <DataGridTextColumn Header="BirthDate" Binding="{Binding DateOfBirth, StringFormat={}{0:yyyyMMdd}}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Background="#090909" BorderBrush="#505661" BorderThickness="1">
                <DataGrid ItemsSource="{Binding Patients}"
                          SelectedItem="{Binding SelectedPatient}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#2a2a2a"
                          BorderThickness="0"
                          RowHeaderWidth="0"
                          Background="#090909"
                          Foreground="#dde6f4"
                          ColumnHeaderHeight="34"
                          RowHeight="40">
                    <DataGrid.ColumnHeaderStyle>
                        <StaticResource ResourceKey="DarkHeaderStyle"/>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <StaticResource ResourceKey="DarkRowStyle"/>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Accession No" Binding="{Binding PatientId}" Width="210"/>
                        <DataGridTextColumn Header="StudyDate" Binding="{Binding CreatedAt, StringFormat={}{0:yyyyMMdd}}" Width="140"/>
                        <DataGridTextColumn Header="StudyDescription" Binding="{Binding Name}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <TextBlock Grid.Column="0" Grid.Row="2" Text="{Binding Patients.Count, StringFormat=Total: {0}}" Foreground="#d5dceb" Margin="8,8,0,0"/>

        <Border Grid.Column="1" Grid.RowSpan="3" Background="#1a2130" BorderBrush="#505661" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="32"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" Height="32">
                        <ComboBoxItem Content="ALL" IsSelected="True"/>
                    </ComboBox>
                    <TextBox Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Margin="6,0,6,0" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="2" Content="&#xE721;" Style="{StaticResource SideIconButtonStyle}" Width="32" Height="32" Margin="0"/>
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <CheckBox Content="Suspend" Foreground="#dce3f1" Margin="0,0,10,0"/>
                    <CheckBox Content="Hide" Foreground="#dce3f1"/>
                </StackPanel>

                <UniformGrid Grid.Row="2" Columns="5" Margin="0,0,0,10">
                    <Button Content="Today" Command="{Binding FilterByPeriodCommand}" CommandParameter="Today" Margin="0,0,4,0"/>
                    <Button Content="3Days" Command="{Binding FilterByPeriodCommand}" CommandParameter="3Days" Margin="0,0,4,0"/>
                    <Button Content="1Week" Command="{Binding FilterByPeriodCommand}" CommandParameter="1Week" Margin="0,0,4,0"/>
                    <Button Content="All" Command="{Binding FilterByPeriodCommand}" CommandParameter="All" Margin="0,0,4,0"/>
                    <Button Content="1Month" Command="{Binding FilterByPeriodCommand}" CommandParameter="1Month"/>
                </UniformGrid>

                <Grid Grid.Row="3" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <DatePicker Grid.Column="0" Height="30"/>
                    <DatePicker Grid.Column="2" Height="30"/>
                </Grid>

                <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="&#xECA5;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE14C;" Style="{StaticResource SideIconButtonStyle}"/>
                </StackPanel>

                <Button Grid.Row="5"
                        Content="Quick Start"
                        Command="{Binding SelectPatientCommand}"
                        CommandParameter="{Binding SelectedPatient}"
                        Background="#df4938"
                        BorderBrush="#df4938"
                        Foreground="White"
                        FontWeight="Bold"
                        Height="46"
                        VerticalAlignment="Top"/>

                <WrapPanel Grid.Row="6" Margin="0,12,0,0">
                    <Button Content="&#xE710;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE70F;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE74D;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE72F;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE721;" Style="{StaticResource SideIconButtonStyle}"/>
                </WrapPanel>

                <WrapPanel Grid.Row="7" Margin="0,8,0,0">
                    <Button Content="&#xE7B5;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE898;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE749;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE958;" Style="{StaticResource SideIconButtonStyle}"/>
                    <Button Content="&#xE1D8;" Style="{StaticResource SideIconButtonStyle}"/>
                </WrapPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
