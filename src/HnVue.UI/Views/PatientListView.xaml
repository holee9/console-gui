<UserControl x:Class="HnVue.UI.Views.PatientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="280">
    <!--
        @MX:NOTE PatientListView — patient/worklist panel (left sidebar, 280px).
                 Implements HRUS-001: patient identification with status badges.
                 Issue #44: DataGrid with icon buttons and CoreTokens.
    -->

    <Grid Background="#0d1527">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header row: title + patient count badge -->
        <Grid Grid.Row="0" Margin="12,12,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Section badge - PPT spec: 12px Bold #f9e04b on #2a3a5c background, padding 2px 8px, radius 3px -->
            <Border Grid.Column="0"
                    Background="#2a3a5c"
                    CornerRadius="3"
                    Padding="8,2"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left">
                <TextBlock Text="WORKLIST"
                           Foreground="#f9e04b"
                           FontSize="12"
                           FontWeight="Bold"
                           FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
            </Border>

            <!-- Patient count badge -->
            <Border Grid.Column="1"
                    Background="#1f6fc7"
                    CornerRadius="10"
                    Padding="7,2"
                    VerticalAlignment="Center">
                <TextBlock Text="{Binding Patients.Count}"
                           Foreground="#e0e6f0"
                           FontSize="11"
                           FontWeight="SemiBold"
                           FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
            </Border>
        </Grid>

        <!-- Search area - PPT spec: 32px toolbar, filter bar 28px -->
        <Border Grid.Row="1"
                Margin="12,0,12,6"
                Height="32"
                CornerRadius="4"
                Background="#1a2540"
                BorderBrush="#3a5080"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Search icon — Segoe MDL2 Assets -->
                <TextBlock Grid.Column="0"
                           Text="&#xE721;"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="14"
                           Foreground="#b8cce0"
                           VerticalAlignment="Center"
                           Margin="10,0,6,0"/>

                <TextBox Grid.Column="1"
                         Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Background="Transparent"
                         Foreground="#e0e6f0"
                         BorderThickness="0"
                         FontSize="13"
                         VerticalAlignment="Center"
                         Padding="0,0,8,0"
                         CaretBrush="#7bc8f5"
                         FontFamily="{DynamicResource HnVue.Core.FontFamily}">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Resources>
                                <VisualBrush x:Key="WatermarkBrush" TileMode="None" Stretch="None" AlignmentX="Left">
                                    <VisualBrush.Visual>
                                        <TextBlock Text="Search patients..."
                                                   Foreground="#7090b0"
                                                   FontSize="13"
                                                   Margin="0"/>
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Style.Resources>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource WatermarkBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
        </Border>

        <!-- Period filter buttons — PPT spec: filter bar height 28px EXPLICIT -->
        <ScrollViewer Grid.Row="2"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled"
                      Margin="12,0,12,6"
                      Height="28">
            <StackPanel Orientation="Horizontal">
                <Button Content="Today"
                        Command="{Binding FilterByPeriodCommand}"
                        CommandParameter="Today"
                        Style="{StaticResource HnVue.OutlineButton}"
                        Height="26"
                        Padding="8,0"
                        Margin="0,0,4,0"
                        FontSize="11"
                        FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
                <Button Content="3Days"
                        Command="{Binding FilterByPeriodCommand}"
                        CommandParameter="3Days"
                        Style="{StaticResource HnVue.OutlineButton}"
                        Height="26"
                        Padding="8,0"
                        Margin="0,0,4,0"
                        FontSize="11"
                        FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
                <Button Content="1Week"
                        Command="{Binding FilterByPeriodCommand}"
                        CommandParameter="1Week"
                        Style="{StaticResource HnVue.OutlineButton}"
                        Height="26"
                        Padding="8,0"
                        Margin="0,0,4,0"
                        FontSize="11"
                        FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
                <Button Content="All"
                        Command="{Binding FilterByPeriodCommand}"
                        CommandParameter="All"
                        Style="{StaticResource HnVue.OutlineButton}"
                        Height="26"
                        Padding="8,0"
                        Margin="0,0,4,0"
                        FontSize="11"
                        FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
                <Button Content="1Month"
                        Command="{Binding FilterByPeriodCommand}"
                        CommandParameter="1Month"
                        Style="{StaticResource HnVue.OutlineButton}"
                        Height="26"
                        Padding="8,0"
                        FontSize="11"
                        FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Action button row: Register -->
        <Grid Grid.Row="3" Margin="12,0,12,8">
            <Button Command="{Binding RegisterPatientCommand}"
                    Style="{StaticResource HnVue.OutlineButton}"
                    Height="32"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE710;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="13"
                               VerticalAlignment="Center"
                               Margin="0,0,5,0"/>
                    <TextBlock Text="Register"
                               FontSize="13"
                               VerticalAlignment="Center"
                               FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Patient list -->
        <ListBox Grid.Row="4"
                 ItemsSource="{Binding Patients}"
                 SelectedItem="{Binding SelectedPatient}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 FontFamily="{DynamicResource HnVue.Core.FontFamily}">
            <ListBox.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding SelectPatientCommand}"
                              CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
            </ListBox.InputBindings>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#e0e6f0"/>
                    <Setter Property="Height" Value="44"/>
                    <Setter Property="Padding" Value="12,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#2a3a58"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#1f6fc7"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#1a2a44"/>
                        </MultiTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Emergency badge — PPT spec: EMRG badge -->
                        <Border Grid.Column="0"
                                Background="#e74c3c"
                                CornerRadius="2"
                                Padding="4,1"
                                Margin="0,0,7,0"
                                VerticalAlignment="Center"
                                Visibility="{Binding IsEmergency, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="EMRG"
                                       Foreground="#e0e6f0"
                                       FontSize="10"
                                       FontWeight="Bold"
                                       FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
                        </Border>

                        <!-- Patient ID and Name -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#e0e6f0"
                                       TextTrimming="CharacterEllipsis"
                                       FontFamily="{DynamicResource HnVue.Core.FontFamily}">
                                <Run Text="{Binding PatientId}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding Name}"
                                       FontSize="12"
                                       Foreground="#b8cce0"
                                       TextTrimming="CharacterEllipsis"
                                       FontFamily="{DynamicResource HnVue.Core.FontFamily}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status area: loading + error -->
        <StackPanel Grid.Row="5" Margin="0,0,0,0">
            <!-- IsLoading indicator — SWR-NF-UX-025 / Issue #16 -->
            <ProgressBar IsIndeterminate="True"
                         Height="3"
                         Background="Transparent"
                         Foreground="#7bc8f5"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <!-- ErrorMessage display — SWR-NF-UX-025 / Issue #16 -->
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#e74c3c"
                       FontSize="11"
                       TextWrapping="Wrap"
                       Margin="12,4,12,4"
                       FontFamily="{DynamicResource HnVue.Core.FontFamily}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
        </StackPanel>

    </Grid>
</UserControl>
