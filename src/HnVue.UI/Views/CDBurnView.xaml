<UserControl x:Class="HnVue.UI.Views.CDBurnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CD/DVD Burn" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Study Instance UID"/>
            <TextBox Text="{Binding SelectedStudyId, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
        </StackPanel>

        <ProgressBar Grid.Row="2" Value="{Binding BurnProgress}" Minimum="0" Maximum="100"
                     Height="20" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,0,0,4"/>

        <!-- ErrorMessage display — Issue #16 -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,4,0,4"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <Button Content="Start Burn" Command="{Binding StartBurnCommand}" Width="100" Margin="0,0,8,0"/>
            <Button Content="Cancel" Command="{Binding CancelBurnCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</UserControl>
