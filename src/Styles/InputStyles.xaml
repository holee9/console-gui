<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Themes/AppTheme.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Base TextBox Style -->
    <Style x:Key="TextBoxBase" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource Neutral.Surface}"/>
        <Setter Property="Foreground" Value="{StaticResource Neutral.Text}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource Size.Body}"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="BorderBrush" Value="{StaticResource Neutral.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CaretBrush" Value="{StaticResource Primary.Main}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Primary.Light}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                            <!-- Focus Indicator -->
                            <Border x:Name="FocusIndicator"
                                    BorderBrush="{StaticResource State.Focus}"
                                    BorderThickness="2"
                                    CornerRadius="6"
                                    Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Focus State -->
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource Primary.Main}"/>
                            <Setter TargetName="FocusIndicator" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <!-- Error State (using Tag) -->
                        <Trigger Property="Tag" Value="Error">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource Semantic.Error}"/>
                        </Trigger>
                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Default TextBox -->
    <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxBase}"/>

    <!-- Search TextBox -->
    <Style x:Key="TextBox.Search" TargetType="TextBox" BasedOn="{StaticResource TextBoxBase}">
        <Setter Property="Padding" Value="36,8,12,8"/>
        <Style.Resources>
            <DrawingBrush x:Key="SearchIcon" Stretch="Uniform">
                <DrawingBrush.Drawing>
                    <GeometryDrawing Brush="{StaticResource Neutral.TextMuted}">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry RadiusX="8" RadiusY="8" Center="8,8"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingBrush.Drawing>
            </DrawingBrush>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <Grid>
                            <!-- Search Icon Placeholder -->
                            <TextBlock Text="🔍"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       FontSize="14"
                                       Foreground="{StaticResource Neutral.TextMuted}"/>
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="36,0,12,0"
                                        VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
