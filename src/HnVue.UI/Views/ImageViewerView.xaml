<UserControl x:Class="HnVue.UI.Views.ImageViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="900">
    <Grid Background="#242424" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="76"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#1a2130"
                BorderBrush="#495364"
                BorderThickness="1"
                CornerRadius="6"
                Margin="0,0,0,10"
                Padding="10">
            <UniformGrid Columns="8">
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
                <Border Margin="4,0" Background="#5e91df"><TextBlock Text="CHEST PA" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,6"/></Border>
            </UniformGrid>
        </Border>

        <Border Grid.Row="1"
                Background="#000000"
                BorderBrush="#4f5663"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Image Source="{Binding ImageSource}"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Visibility="{Binding IsImageLoaded, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Visibility="{Binding IsImageLoaded, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <TextBlock Text="&#xEB9F;"
                               FontFamily="Segoe MDL2 Assets"
                               Foreground="#6d87aa"
                               FontSize="64"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,14"/>
                    <TextBlock Text="No image loaded"
                               Foreground="#8ea2bf"
                               FontSize="18"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
