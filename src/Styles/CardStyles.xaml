<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Themes/AppTheme.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Base Card Style (Border) -->
    <Style x:Key="CardBase" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Neutral.Surface}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#000000" Opacity="0.3" BlurRadius="12" ShadowDepth="0" Direction="0"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Card Style -->
    <Style x:Key="Card" TargetType="Border" BasedOn="{StaticResource CardBase}">
        <Setter Property="Margin" Value="8"/>
    </Style>

    <!-- Hoverable Card -->
    <Style x:Key="Card.Hoverable" TargetType="Border" BasedOn="{StaticResource Card}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF2E2E52"/>
                <Setter Property="BorderBrush" Value="{StaticResource Primary.Light}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Selected Card -->
    <Style x:Key="Card.Selected" TargetType="Border" BasedOn="{StaticResource Card}">
        <Setter Property="BorderBrush" Value="{StaticResource Primary.Main}"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <!-- Card Content Styles -->
    <Style x:Key="CardTitle" TargetType="TextBlock" BasedOn="{StaticResource TextStyle.H2}">
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style x:Key="CardText" TargetType="TextBlock" BasedOn="{StaticResource TextStyle.Body}">
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="CardCaption" TargetType="TextBlock" BasedOn="{StaticResource TextStyle.Caption}">
        <Setter Property="Margin" Value="0,4,0,0"/>
    </Style>

</ResourceDictionary>
