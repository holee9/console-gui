<UserControl x:Class="HnVue.UI.Views.PatientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Patient List" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     Width="300" Margin="0,0,8,0"/>
            <Button Content="Search" Command="{Binding SearchCommand}" Width="80"/>
            <Button Content="Register" Command="{Binding RegisterPatientCommand}"
                    Width="80" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- IsLoading indicator — SWR-NF-UX-025 / Issue #16 -->
        <ProgressBar Grid.Row="2" IsIndeterminate="True" Height="4" VerticalAlignment="Top"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- ErrorMessage display — SWR-NF-UX-025 / Issue #16 -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Top"
                   Margin="0,8,0,0"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

        <!-- Patient list — SelectPatientCommand bound via InputBindings / Issue #15 -->
        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Patients}"
                 SelectedItem="{Binding SelectedPatient}"
                 Margin="0,20,0,0">
            <ListBox.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding SelectPatientCommand}"
                              CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
            </ListBox.InputBindings>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <!-- Emergency badge — SWR-PM-013 / Issue #15 -->
                        <Border Background="Red" CornerRadius="2" Padding="3,1" Margin="0,0,6,0"
                                Visibility="{Binding IsEmergency, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="EMRG" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <TextBlock>
                            <Run Text="{Binding PatientId}"/>
                            <Run Text=" — "/>
                            <Run Text="{Binding Name}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
