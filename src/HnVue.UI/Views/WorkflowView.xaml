<UserControl x:Class="HnVue.UI.Views.WorkflowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HnVue.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <converters:SafeStateToColorConverter x:Key="SafeStateToColor"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Acquisition Workflow" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <!-- SafeState Indicator Bar — Issue #31 / SWR-NF-SC-041 -->
        <Border Grid.Row="1"
                Background="{Binding CurrentSafeState, Converter={StaticResource SafeStateToColor}}"
                CornerRadius="4"
                Padding="8,4"
                Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="System State: "
                           Foreground="White"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding SafeStateLabel}"
                           Foreground="White"
                           FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- Workflow State -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="State: " FontWeight="SemiBold"/>
            <TextBlock Text="{Binding CurrentState}"/>
        </StackPanel>

        <!-- Status Message -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Top">
            <Button Content="Prepare" Command="{Binding PrepareExposureCommand}" Width="100" Margin="0,0,8,0"/>
            <Button Content="Expose" Command="{Binding TriggerExposureCommand}" Width="100" Margin="0,0,8,0"/>
            <Button Content="Abort" Command="{Binding AbortCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>
